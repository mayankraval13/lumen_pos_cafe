PORT=3001
DATABASE_URL=postgresql://localhost:5432/pos_cafe
JWT_SECRET=change-this-to-a-long-random-secret
NODE_ENV=development

# Resend (https://resend.com) — signup OTP only (free tier: low daily limit; use a verified domain for production)
RESEND_API_KEY=
RESEND_FROM=

# Optional: log OTP to server console in development if email delivery fails (never enable in production)
# SIGNUP_OTP_DEV_LOG=true
